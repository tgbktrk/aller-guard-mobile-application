import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.tugba',
  appName: 'allergen-demo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
